import { NextResponse } from 'next/server';import { createClient,createServiceRoleClient } from '@/lib/supabase/server';import { requireSquadInvitePermission } from '@/lib/require-squad-invite-permission';import { isSquadInviteMvpEnabled } from '@/lib/squad-invite-mvp';
export async function POST(_request:Request,{params}:{params:{id:string;outboxId:string}}){if(!isSquadInviteMvpEnabled())return NextResponse.json({error:'Not found'},{status:404});const staff=await requireSquadInvitePermission(createClient(),'squad_invite_approver');if(!staff.ok)return NextResponse.json({error:staff.error},{status:staff.status});const service=createServiceRoleClient();const {data:owned}=await service.from('squad_invite_notification_outbox').select('id').eq('id',params.outboxId).eq('request_id',params.id).maybeSingle();if(!owned)return NextResponse.json({error:'Notification unavailable'},{status:404});const {data,error}=await service.rpc('prepare_squad_invite_notification_resend',{p_outbox_id:params.outboxId,p_staff_profile_id:staff.userId});if(error||!data)return NextResponse.json({error:'Resend unavailable'},{status:409});return NextResponse.json(data);}
