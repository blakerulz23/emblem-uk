import { readFileSync } from 'fs';import { describe,expect,it } from 'vitest';
const read=(p:string)=>readFileSync(p,'utf8');
describe('Squad Invite MVP organiser, staff and preview contracts',()=>{
 it('gates every new production surface behind the closed runtime flag',()=>{for(const p of ['src/app/squad-invite/start/page.tsx','src/app/squad-invite/manage/[reference]/page.tsx','src/app/api/squad-invite-requests/route.ts','src/app/api/squad-invite-requests/[reference]/activate/route.ts','src/app/api/staff/squad-invites/[id]/approve/route.ts'])expect(read(p)).toContain('isSquadInviteMvpEnabled');});
 it('blocks parent participation and every real Squad Invite surface when the runtime flag is closed',()=>{const middleware=read('src/middleware.ts');expect(middleware).toContain('realSquadInviteSurface && !isSquadInviteMvpEnabled()');expect(middleware).toContain("path.startsWith('/api/squad-invite')");});
 it('implements generic OTP handling, resend delay and safe fixed return',()=>{const ui=read('src/app/squad-invite/start/OrganiserStart.tsx');const request=read('src/app/api/squad-invite-organiser-auth/request-code/route.ts');const verify=read('src/app/api/squad-invite-organiser-auth/verify-code/route.ts');expect(ui).toContain('setCooldown(60)');expect(request).toContain('If the address can receive a code');expect(request).toContain("'otp-request'");expect(verify).toContain("'otp-verify'");expect(verify).toContain("returnTo:'/squad-invite/start'");});
 it('rejects prohibited and unknown request fields through a typed allowlist',()=>{const route=read('src/app/api/squad-invite-requests/route.ts');expect(route).toContain('Object.keys(body).some');for(const forbidden of ['childName','photo','roster','dateOfBirth','school','medical','parentContacts'])expect(route).not.toContain(`'${forbidden}'`);});
 it('keeps delivery setup organiser-owned, CSRF checked and absent from email rendering',()=>{const route=read('src/app/api/squad-invite-requests/[reference]/activate/route.ts');const email=read('src/lib/squad-invite-email-preview.ts');expect(route).toContain('hasValidSquadInviteCsrf');expect(route).toContain('r.organiser_profile_id!==user.id');expect(email).not.toMatch(/delivery_address|courierContact|postcode/);});
 it('enforces reviewer and approver permissions on server routes',()=>{const review=read('src/app/api/staff/squad-invites/[id]/review/route.ts');const approve=read('src/app/api/staff/squad-invites/[id]/approve/route.ts');expect(review).toContain("'squad_invite_reviewer'");expect(review).toContain("'squad_invite_approver'");expect(approve).toContain("'squad_invite_approver'");});
 it('contains all twenty isolated synthetic review states and no service imports',()=>{const preview=read('src/app/dev/squad-invite-preview/SquadInvitePreview.tsx');for(const state of ['homepage-entry','builder-entry','start','organiser-email','organiser-otp','organiser-form','organiser-declarations','organiser-review','request-received','staff-queue','staff-review','changes-requested','organiser-correction','approval-foundation','approval-email','setup-required','delivery-setup','link-ready','notification-failure','feature-disabled'])expect(preview).toContain(`'${state}'`);expect(preview).not.toMatch(/supabase|resend\.com|shopify/i);});
 it('documents static-only migration status without claiming DPIA approval',()=>{const dpia=read('docs/compliance/childrens-dpia-v0.1.md');expect(dpia).toContain('STATICALLY VERIFIED ONLY — NOT EXECUTED');expect(dpia).toContain('DPIA remains unapproved');});
});
